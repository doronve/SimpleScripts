#!/bin/bash
source params.sh

az group create --location ${LOCATION} \
                --name ${GROUPNAME} \
                --tags ${TAGS} \


exit
az group create --location
                --name
                [--managed-by]
                [--tags]


